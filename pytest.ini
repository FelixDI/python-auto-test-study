[pytest]
# 搜索路径
testpaths = src/stage1_pytest_core/test_cases

# 命名规则
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 默认参数
addopts = -v --tb=short --strict-markers

# 标记注册
markers =
    smoke: 冒烟测试
    slow: 耗时用例