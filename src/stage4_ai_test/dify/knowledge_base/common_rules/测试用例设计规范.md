# 测试用例设计规范
## 一、用例结构标准
每条测试用例必须包含：用例ID、用例名称、前置条件、测试步骤、预期结果
## 二、用例编号规则
- 接口用例：API_模块名_序号（例：API_Product_001）
- UI用例：UI_页面名_序号（例：UI_Login_001）
## 三、场景覆盖要求
1. 正常场景：符合业务规则的正向操作
2. 异常场景：参数错误、权限缺失、数据不存在等反向场景
3. 边界场景：参数临界值、数据量边界等
## 四、输出格式要求
统一使用 Markdown 分级列表+表格的结构化格式输出，用例描述清晰无歧义