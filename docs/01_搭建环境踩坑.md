# git 推送报错 新手规避

```
# 初学者经常写测试脚本、练手项目，建议新建项目时
# 默认不勾选 Create Git repository
# 只有在确定需要版本控制或推送到 GitHub 时，才手动：
# 创建 .gitignore
# 执行 VCS → Enable Version Control Integration → Git
# 按正常流程add commit首次提交、
# GitHub 上新建一个完全空白的仓库（不勾选 README、不勾选.gitignore、不勾选 License）
# 关联远程仓库 SSH 地址,push推送
# 这样既避免了不必要的 Git 初始化，又不会因为忘记 .gitignore 而踩坑。
```

# docker desktop + FastAPI
```
第三方镜像存在不确定性，各种拉取失败.
直接用官方 Python 基础镜像 + FastAPI 代码

```
# docker-compose.yml 管理部署镜像、容器
```
Jenkins 容器，第一次进去安装插件 很多失败
登录进去之后 右上角⚙️Manage jenkins 
Some plugins could not be loaded due to unsatisfied dependencies. 
Fix these issues and restart Jenkins to re-enable these plugins.
然后 correct
优先将排在上面的插件都安装

可以走代理,下载网络更稳定，把代理环境变量传进去
environment:
    - HTTP_PROXY=http://host.docker.internal:7897
    - HTTPS_PROXY=http://host.docker.internal:7897
    
PythonAutoTest
├── src
├── jenkins_home
│   └── workspace
│       ├── SauceDemo_UI_Test
│       ├── 电商接口自动化测试
│       └── ecommerce_api_test

volumes:
  - ./mysql_data:/var/lib/mysql     # 数据持久化
  - ./jenkins_home:/var/jenkins_home # Jenkins 配置持久化
环境混乱 建议如下

~/Projects/
└── PythonAutoTest

~/DockerData/
├── jenkins_home
└── mysql_data
```