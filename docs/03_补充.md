# mysql基础
```
CREATE DATABASE   ;
USE    ;

CREATE TABLE  ();

SELECT * FROM  ;
INSERT INTO ()  VALUES();
UPDATE SET   ;
DELETE FROM   ;
```
# linux常见命令的练习
```
cd
cd ..
pwd
ls
cat
grep
exit
```

# docker
```
docker exec -it mysql-test mysql -u -p
docker exec -it -u root mysql-test bash

```

# git
```
clone	把远程仓库下载到本地
status	查看当前文件状态（改了哪些、新增了哪些）
add	把文件加入暂存区，准备提交
commit	把暂存区的文件正式提交到本地仓库
push	把本地提交推送到 GitHub
pull	拉取远程仓库的最新代码
branch	创建分支、切换分支
.gitignore	忽略不需要版本控制的文件/目录
```

# Allure
```
pytest xxx.py -v --alluredir=reports/allure-results

allure serve reports/allure-results

allure generate reports/allure-results -o reports/allure-report --clean
allure open reports/allure-report

allure.feature()
class ...
    allure.story()
    allure.title()
    def ...
    allure.step()
```

# fiddler
```
浏览器网站配置端口 进行录制 RAW格式查看 然后Replay->Reissue and Edit->Run
```

# postman实操 接口调试
```
add Environments
add collection
add request
run the collection

code: Python-Requests
```